name: Playwright E2E

on:
  pull_request:
  push:
    branches:
      - main

# Read-only on the repo (only `checkout` needs it); this job runs the E2E suite
# and reports a status, never writing back to git. Least-privilege token,
# matching otbeat-ingest.yml.
permissions:
  contents: read

# Cancel a stale run when a newer commit is pushed to the same PR ref —
# successive fixups shouldn't burn parallel runners when only the latest
# matters. Gated to non-main refs so every commit on `main` keeps a completed
# CI verdict (back-to-back merges must not cancel an earlier commit's run).
concurrency:
  group: e2e-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          # Read-only job — don't leave the GITHUB_TOKEN in .git/config.
          persist-credentials: false

      - name: Set up Node.js and install dependencies
        uses: ./.github/actions/node-setup

      # WebKit as well as Chromium (#359): the `mobile-webkit` project runs the
      # rendering specs on an iPhone 14 profile, which is the only place the
      # suite exercises a non-Chromium engine or a phone viewport — the two
      # gaps that let #353 and #355/#356 ship. `--with-deps` pulls WebKit's
      # system libraries, which is the bulk of the added setup time; the run
      # itself reuses the dev servers the other projects already start.
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium webkit

      - name: Run Playwright smoke suite
        run: npm run test:e2e
